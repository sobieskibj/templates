defaults:
  - _self_
  - network: test
  - dataloader: test
  - wandb: default
  - metric: all_test
  - hydra: default
  - fabric: default

exp:
  seed: 42
  log_dir: # set during runtime to automatically created dir
  run_func: 
    _target_: experiment.test.run